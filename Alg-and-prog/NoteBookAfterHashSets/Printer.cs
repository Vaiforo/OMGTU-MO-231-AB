public class Printer {
    static public void PrintMostPopular(Dictionary<string, Call> history, string mostPopularNumber, string fromNumber) {
        Console.WriteLine($"С номера {fromNumber} чаще всего звонили на номер {mostPopularNumber}");
        foreach (var (date, call))
    }
}